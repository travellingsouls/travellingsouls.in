import type { GalleryItem } from "@/lib/types";

/**
 * INTENTIONALLY EMPTY until real photographs are supplied.
 *
 * The brief asks for authentic travel photography and rules out generic stock.
 * It also rules out using copyrighted images without permission, so no image
 * is added here until its source and licence are known.
 *
 * Photographs will be uploaded to Supabase Storage in PHASE 8 under
 * travel-images/gallery/, and every one needs meaningful alt text.
 */
export const galleryMock: GalleryItem[] = [];
